# Houdini Code Mode handoff — 2026-08-12

## Executive state

The standalone Code Mode product is viable and has moved well beyond the first
vertical slice. It exposes exactly one model-facing MCP tool,
`houdini_code_run`, backed by the same controller as the human/debug CLI. A
submitted program executes on Houdini's main thread with fresh `hou`, `ctx`,
`args`, and `result` globals and returns one bounded structured envelope.

The current automated suite collects 155 tests: **155 passed** with
`HOUDINI_CODEMODE_SECOND_PORT=18814` (`uv run pytest`, 18.73s). The verified
live targets are two local Houdini 22.0.368/Python 3.11 sessions on loopback
ports 18811 and 18814; the host-orchestrated transfer proof runs between them.

The original CLI repo at
`D:\vibe_code\00_houdini_projects\houdini_CLI` is clean on branch `master` at
tag `v0.3.2`. No private cross-repo imports or editable dependencies were
introduced.

Important repository state: the standalone repo at
`D:\vibe_code\00_houdini_projects\houdini_codemode` is on `main`. Baseline
commit `627f837` contains bounded references and the first safe HDA mutations;
the following coherent checkpoint contains the transfer, recipe, HDA
create/update/interface/section/tool work described here.

## Product and protocol decisions

- Keep Code Mode as a separate repo and product from `houdini-cli`.
- Keep one MCP tool rather than mirroring HOM as hundreds of tools.
- Use raw `hou` for API completeness and `ctx` only for bounded projections,
  compound workflows, and audited effects.
- Keep model recursion, provider choice, sessions, and notebook memory above
  the Houdini boundary. The scene is the durable state; user globals are fresh
  per run.
- Keep the human/debug CLI (`check`, `run`, `doctor`) and the operator-only
  `xfer copy` command even though the model sees one tool.
- Treat execution as trusted local code, not a sandbox.
- Marshal whole programs to `MainThread`. Arbitrary Python is not safely
  preemptible; a wait timeout means completion is unknown, not cancellation.
- Never save the HIP implicitly. Undo grouping organizes history but is not a
  transaction or rollback mechanism.

## Implemented runtime

The runtime/controller now provides:

- versioned request, response, error, policy, runtime, and protocol contracts;
- source/result/response/log/string/container/depth/total-item byte and item
  limits;
- Unicode-aware bounded stdout/stderr and exactly one `result.emit()`;
- structured compile/execution/result/busy/connection errors;
- netref-free normalization with compact node/parm/type summaries;
- a Houdini-side non-blocking run lock plus the legacy-compatible Windows
  endpoint mutex namespace;
- a background waiter that keeps the local endpoint gate after caller timeout;
- cached runtime installation by version/source hash;
- exact helper-owned mutation events and honest best-effort direct-HOM tracking;
- a Code Mode-owned loopback shelf bootstrap.

Current `ctx` services are discoverable at runtime with:

```python
result.emit(ctx.capabilities())
```

Targeted exact signatures/effect categories are returned by:

```python
result.emit(ctx.help("ctx.hda.plan_update"))
```

One static host registry generates the embedded discovery data. Tests compare
the registry with every public service and method to catch drift.

## Implemented semantic extensions

- `ctx.nodes`: bounded summaries, descendants, filtering, neighbors, and
  network summaries.
- `ctx.parms`: bounded listing/filtering and caller-ordered projection.
- `ctx.parm_references`: bounded generic dependency auditing through
  `references(node, descendants=False, external_to=None, max_nodes=1000,
  max_parms=10000, max_results=1000, max_errors=100)`. It combines direct HOM
  references with bounded `ch*()` expression/raw fallback and reports internal,
  external, absolute-internal, and unresolved rows.
- `ctx.geometry`: topology summaries, attribute definitions, and bounded
  samples.
- `ctx.cop`: Copernicus output metadata and pixel sampling.
- `ctx.cop_files`: transactional, byte-capped raw/view export and bounded File
  COP import. Export renders to a temporary sibling, validates it, and only then
  atomically replaces the final path; failures preserve existing files.
- `ctx.lop`: bounded USD-stage traversal, counts, type histograms, paths,
  composition, and render-context metadata.
- `ctx.opencl`: validate/sync across SOP, COP, and DOP with connection/value
  preservation behavior.
- `ctx.python`: Python SOP/COP `#bind` inspect/validate/sync.
- `ctx.wrangle`: spare-parameter sync/clear.
- `ctx.artifacts`: manifest-only `.asData` export/inspect/import/list/remove.
  Narrow value/parm/input inverses are used only when the captured root scope
  contains nothing broader; normal full node artifacts correctly use
  `setFromData` because they also capture flags, position, inputs, and other
  state.
- `houdini-codemode xfer copy`: host orchestration, not a runtime or MCP
  extension. It requires explicit distinct loopback source/destination ports,
  preflights the shared artifact root and byte ceiling, uses a UUID artifact,
  restores under the explicit destination parent/name, and returns artifact
  cleanup state plus `hip_saved=False` for both endpoints.
- `ctx.hda`: bounded instance/definition/library inspection, H22-compatible
  external-reference auditing, strict/optional reference validation,
  fresh-instance/frame-cook/interface validation, no-effect parameter promotion
  planning, and no-effect package/update/copy planning. It also exposes the
  deliberately narrow durable mutations
  `package_copy(node, destination_library, type_name=None, label=None,
  overwrite=False, backup=False)` and
  `apply_promotion(node, internal_parms, destination_names, folder=None,
  max_items=25, allow_library_write=False, owned_library=None,
  create_backup=True)`.
- `ctx.hda_sections`: bounded UTF-8 plain-text section read/plan/set/delete in
  one explicitly owned sole-instance library; managed Tools.shelf content is
  intentionally excluded.
- `ctx.hda_tools`: bounded tool discovery and structured SOP/COP Tools.shelf
  set/remove; it does not expose a general opaque shelf-XML API.
- `ctx.hda_interface`: bounded declarative float/int/string/toggle/menu
  templates plus explicit supported tuple defaults-from-current. Folders,
  ramps, callbacks, and multiparms are excluded.
- `ctx.hda_update.update_owned`: constrained whole-definition update for one
  unlocked sole instance in an explicitly owned library, with optional
  whole-interface copy, bounded text section/tool preservation, verified
  backup, optional match/validation, no automatic rollback claim, and no HIP
  save.
- `ctx.hda_create.create_owned`: new external library only, explicit non-HDA
  source node, bounded type/label/input metadata, and honest reporting of
  `createDigitalAsset` installation and source-node type conversion.
- `ctx.recipes`: bounded recipe metadata and script-suppressed node/parameter
  preset application. Authoring and tool/decoration apply are excluded.

## Live evidence and scene state

Live tests prove main-thread dispatch, bounded results/errors, isolated
mutation/cleanup, display/render flags, undo semantics, artifacts, OpenCL
SOP/COP/DOP sync, Python bindings, wrangles, geometry/Cop/LOP summaries, HDA
inspection/reference/validation/planning, Cop image export/import, MCP in-memory
and stdio adapters, generic parameter-reference classification, safe staged HDA
package copy, durable isolated HDA promotion, and real competing-process
serialization.

The owned-update live proof checkpointed a disposable unlocked instance,
persisted new contents and its explicit whole interface, preserved bounded
text `Readme` and `Tools.shelf` sections, validated a fresh locked instance,
and removed the nodes, library, and verified backup without saving the HIP.

The host transfer is live-proven from `localhost:18811` to
`localhost:18814`: it copied a throwaway geometry network with `box1 -> OUT`,
preserved its size and output flags, verified the restored destination, removed
the temporary artifact from both endpoint views, and removed the test nodes in
the test's `finally`. The transfer contract itself intentionally leaves a
successful restored destination node, so the destination HIP can become dirty;
neither endpoint HIP was saved.

The HDA update planner was checked against a real installed HDA library by
recording its path, mtime, size, and HIP dirty state before and after. They were
unchanged. The planner reports future definition/library effects without
calling `updateFromNode`, `save`, `copyToHDAFile`, `matchCurrentDefinition`,
library install, or HIP save.

The generic parameter-reference audit is live-proven on Houdini 22.0.368 for
relative internal, absolute internal, and external `ch()` references. It
excludes `getReferencedParm()` self-results for ordinary parameters, preventing
them from consuming a bounded result budget.

`ctx.hda.package_copy` is live-proven as a staged `copyToHDAFile` followed by
atomic publication: its destination library must be explicit, external to the
source library, and not currently loaded. It does not install the destination,
modify the source instance, or save the HIP; rejected writes preserve the
existing destination and temporary sibling files are cleaned up.

`ctx.hda.apply_promotion` is live-proven only for an isolated disposable HDA:
the caller must pass `allow_library_write=True`, name an existing regular
`owned_library` that exactly matches the definition library, and the runtime
must verify the instance is the sole instance of that definition. It refuses
embedded and HFS/SideFX-installed libraries. It records interface, channel,
`updateFromNode`, and library effects and never saves the HIP. Before
`matchCurrentDefinition`, it checkpoints the current unlocked contents and
replans so unrelated pending edits are not discarded. Its rollback is
not transactional after `updateFromNode`: a backup can restore the library
file, but already-updated definition/instance state cannot be claimed as rolled
back.

The open file is `C:/Users/Scy/Desktop/codemode.hip`. It was already dirty and
remains dirty. It was never saved. The final cleanup audit found:

- no `codemode_*` nodes under `/obj`, `/img`, or `/stage`;
- no Code Mode artifacts;
- no remaining `houdini-codemode-*.exr` probe outputs in the temp directory.

## Skill and MCP guidance design

The MCP metadata intentionally contains only the universal operating contract:
one complete program, fresh globals, JSON args, one compact emission, raw `hou`,
runtime discovery, trusted execution, no implicit save, and unknown-completion
recovery. It does not contain the entire API catalogue.

The bundled `skills/houdini-codemode` skill owns behavior and judgment:

- prefer Code Mode over GUI clicks, legacy `houdini-cli eval`, or repeated tiny
  calls;
- compose discovery, edits, validation, and evidence into one program;
- choose native nodes vs VEX/OpenCL/Python appropriately;
- enforce Copernicus coordinate/binding rules and solver/HDA cautions;
- use prepared, version-matched Houdini/VEX help rather than guessing;
- clean temporary scene/file effects and handle timeout recovery honestly.

Durable CLI skill knowledge was migrated into progressive-disclosure references
for Copernicus, native Cop kernels, OpenCL SOPs, Gas OpenCL/DOPs, and recipes.
Legacy command examples were rewritten as complete Code Mode programs.

The help-extraction script is included at
`skills/houdini-codemode/scripts/prepare_houdini_help.py`. Only the generated
`references/help_prepared/` corpus is excluded and ignored. The default command
is:

```powershell
python <installed-skill>\scripts\prepare_houdini_help.py `
  --source "<houdini-install>\houdini\help"
```

The skill passed the official quick validator, its script `--help` works, and a
dry forward-test correctly produced one composed, no-save OpenCL COP workflow
while refusing to guess absent version-sensitive help.

## Verification commands

Run from `D:\vibe_code\00_houdini_projects\houdini_codemode`:

```powershell
uv sync
uv run pytest -q
uv run houdini-codemode doctor
uv run houdini-codemode run --code "result.emit(ctx.capabilities())"
python C:\Users\Scy\.codex\skills\.system\skill-creator\scripts\quick_validate.py skills\houdini-codemode
uv build
```

To run the endpoint-isolation test with the older environment-variable hook:

```powershell
$env:HOUDINI_CODEMODE_SECOND_PORT = "18814"
uv run pytest tests/test_live_integration.py -k distinct_ports
```

For the public operator transfer, after creating `/obj/source` on port 18811:

```powershell
uv run houdini-codemode xfer copy /obj/source --to-parent /obj `
  --from-port 18811 --to-port 18814 --name restored --children
```

## Remaining checklist

The authoritative list is `docs/CHECKLIST.md`. The meaningful remaining work is:

1. Test another Houdini/Python version and repeat artifact/runtime live coverage.
2. Extend HDA mutation only after preserving the current narrow boundaries:
   broad create/install lifecycle and non-isolated interface writes are still
   not supported; folders, ramps, callbacks, and multiparms remain excluded.
3. Keep recipe authoring and tool/decoration application separate from the
   bounded, script-suppressed node/parameter preset surface.
4. Keep explicit library-write intent, ownership checks, pre/post library
   manifests, exact mutation events, and no-HIP-save guarantees on every new
   HDA file effect. Do not call promotion transactional after `updateFromNode`.
5. Dogfood before extracting shared code. If extraction becomes justified,
   publish a versioned core; never import private modules across repos.
6. Continue using coherent checkpoint commits and keep live probes disposable.

## Recommended next implementation slice

Do not broaden the completed HDA mutations by treating them as general lifecycle
operations. The next slice must separately specify create/install behavior,
multi-instance effects, rollback semantics, and durable ownership policy;
`package_copy`, isolated promotion, bounded sections/tools, and the declarative
interface do not authorize those operations.
